// (function (Drupal, once) {
//   Drupal.behaviors.front = {};
//   Drupal.behaviors.front.attach = (context: HTMLElement) => {
//     console.log('Frontend', context, once);
//   };
// })(Drupal, once);

export {};
