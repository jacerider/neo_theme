
// (function (Drupal, once) {

//   Drupal.behaviors.back = {};
//   Drupal.behaviors.back.attach = (context: HTMLElement) => {
//     console.log('Backend', context, once);
//   }

// })(Drupal, once);

export {};
