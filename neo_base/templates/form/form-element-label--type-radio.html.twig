{% extends 'form-element-label.html.twig' %}

{%
  set classes = [
    'text-base',
    'cursor-pointer',
    neo_size == 'lg' ? 'text-lg',
    neo_size == 'sm' ? 'text-sm',
    neo_size == 'xs' ? 'text-xs',
  ]
%}
