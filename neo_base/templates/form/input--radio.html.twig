{% extends 'input.html.twig' %}

{%
  set classes = [
    'border-2',
    'w-5',
    'h-5',
    'text-primary-500',
    'bg-base-100',
    'border-base-400',
    'hover:border-base-600',
    'focus:ring',
    'focus:ring-primary-600',
    'focus:ring-offset-2',
    'transition',
    disabled ? 'cursor-not-allowed' : 'cursor-pointer',
    neo_style == 'inline_buttons' ? 'sr-only',
    neo_style == 'inline_elements' ? 'ml-1',
    neo_style == 'grid_buttons' ? 'sr-only',
  ]
%}
