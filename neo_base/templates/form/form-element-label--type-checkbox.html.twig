{% extends 'form-element-label.html.twig' %}

{%
  set classes = [
    'text-base',
    'text-sm',
    'ml-1.5',
    'cursor-pointer',
    neo_size == 'lg' ? 'text-lg',
    neo_size == 'md' ? 'text-md',
    neo_size == 'sm' ? 'text-sm',
    neo_size == 'xs' ? 'text-xs',
  ]
%}
