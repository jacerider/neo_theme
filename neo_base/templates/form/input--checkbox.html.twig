{% extends 'input.html.twig' %}

{%
  set classes = [
    'border-2',
    'border-base-400',
    'cursor-pointer',
    'hover:border-base-600',
    'rounded',
    'bg-base-100',
    'checked:bg-primary-500',
    'text-primary-500',
    'focus:ring',
    'focus:ring-primary-600',
    'focus:ring-offset-2',
    'disabled:opacity-50',
    'p-2',
    'transition',
    neo_style == 'inline_buttons' ? 'sr-only',
    neo_style == 'inline_elements' ? 'ml-2',
    neo_style == 'grid_buttons' ? 'sr-only',
  ]
%}
