{% extends 'fieldset.html.twig' %}

{%
  set classes = [
    'text-base',
    'form--item',
    neo_style == 'default' ? 'card',
    legend.title is empty or description_display == 'before' and description.content is empty ? 'pt-0',
  ]
%}
{%
  set legend_classes = []
%}
{%
  set legend_span_classes = [
    'text-base',
    '[&>.form-item]:my-0',
    neo_size == 'md' ? 'font-bold',
    neo_size == 'sm' ? 'font-semibold text-sm',
    neo_size == 'xs' ? 'font-semibold text-xs',
  ]
%}
{%
  set content_classes = [
    '[&>p]:my-form-item',
    '[&>p]:text-sm',
    '[&>.field-prefix]:text-sm',
    '[&>.field-prefix]:block',
    '[&>.field-prefix]:mb-2',
  ]
%}
{%
  set spacing_classes = [
    title_display != 'invisible' ? 'mt-form-item',
  ]
%}
